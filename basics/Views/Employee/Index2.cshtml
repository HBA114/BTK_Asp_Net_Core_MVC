@{
    ViewData["Title"] = "Employee Page";
}

@model string[]

<div>
    <h3 class="display-3">
        Employee
    </h3>
    <div class="lead">
        @foreach (string name in Model)
        {
            <p>@name</p>
        }
    </div>
</div>
